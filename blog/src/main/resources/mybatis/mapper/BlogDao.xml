<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.wyy.dao.BlogDao">
    <select id="getBlogById" resultType="Blog">
        select * from t_blog where id = #{id}
    </select>
    <select id="getBlogsByCondition" resultType="Type">
        select * from t_blog where name = #{name} and 
    </select>
    <select id="getAllTypes" resultType="Type">
        select * from t_type
    </select>
    <insert id="saveType" parameterType="Blog">
        insert into t_type(name) values(#{name})
    </insert>
    <update id="updateType" parameterType="Type">
        update t_type set name = #{name} where id = #{id}
    </update>
    <delete id="deleteType">
        delete from t_type where id = #{id}
    </delete>
</mapper>